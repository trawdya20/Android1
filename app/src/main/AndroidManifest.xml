<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uty.arffestofoodandbeverages">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArffestoFoodAndBeverages">
        <activity android:name=".fragment3.DetailFragment3"></activity>
        <activity android:name=".fragment1.DetailFragment" />
        <activity android:name=".DetailActivityMinum" />
        <activity android:name=".MainActivityMinum" />
        <activity android:name=".DetailActivityMakan" />
        <activity android:name=".MainActivityMakan" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>